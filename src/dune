(library
 (name        borderline_lib)
 (libraries   base stdio str ounit2 ipaddr menhirLib)
 (synopsis "")
 (inline_tests)
 (preprocess (pps ppx_expect ppx_compare ppx_deriving.show ppx_sexp_conv))
)

(menhir
 (modules parser))
; MENHIR_FLAGS = --explain --infer

(ocamllex lexer)
