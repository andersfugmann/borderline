import "./test"

zone self {
     interface = lo;
}

define self = rule { rule { source zone = self } policy allow } policy deny


process filter {
        rule { state = related, established; } policy allow;
        rule { state = new; call zones; } policy deny;
} policy deny
