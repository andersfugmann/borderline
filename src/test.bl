import "./test"

zone self {
     interface = lo;
     process filter { rule { source zone = self } policy allow } policy deny
}

process filter {
        rule { 
             rule { state = related, established; } policy allow;
             state = new; 
             call zones;
        } policy deny
} policy deny

